# Admin dashboard environment variables.
# Copy ke .env.local untuk override.

# API base URL — kosongin atau set ke URL backend NestJS.
# Kalau kosong / backend offline → admin pakai mock data + dev creds.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/v1
